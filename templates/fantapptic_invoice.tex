\documentclass{article}
\usepackage[utf8x]{inputenc}
\usepackage{lmodern, textcomp}
\usepackage{geometry}
\usepackage{fancyhdr}

\geometry{
    a4paper,
    top=30mm,
    bottom=30mm,
    left=20mm,
    right=20mm,
}

\pagestyle{fancy}
\fancyhf{}
\lhead{Fantapptic Apps.}
\rhead{Rechnung vom $date}

\begin{document}

\begin{center}
    \begin{tabular}{ l r }
        \hline
        #for $a in $articles
        $a.name & $a.price \\ \hline
        #end for
        \hline
    \end{tabular}
\end{center}

\end{document}
